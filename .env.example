# --- Frontend (bundled into the browser; safe to expose) ---
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_APP_ORIGIN=

# --- Server only (Vercel api/ functions + edge function); never VITE_-prefixed ---
RESEND_API_KEY=
WEBHOOK_SECRET=
ADMIN_EMAIL=

# Service-role access for the Bunny endpoints (verify JWT, check enrollment).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# --- Bunny Stream video hosting (server only) ---
BUNNY_API_KEY=
BUNNY_LIBRARY_ID=
BUNNY_CDN_HOSTNAME=
BUNNY_TOKEN_AUTH_KEY=
